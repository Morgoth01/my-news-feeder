<Window x:Class="MyNewsFeeder.Views.ArticleWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:wv="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Article"
        Height="760"
        Width="980"
        MinHeight="520"
        MinWidth="720"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{DynamicResource MaterialDesignFont}">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="10"
                Margin="0,0,0,8"
                Background="{DynamicResource MaterialDesignCardBackground}"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="1"
                CornerRadius="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            Margin="0,0,12,0">
                    <TextBlock x:Name="ArticleTitleText"
                               FontWeight="SemiBold"
                               FontSize="16"
                               TextWrapping="Wrap"/>
                    <TextBlock x:Name="ArticleLinkText"
                               Margin="0,4,0,0"
                               Foreground="{DynamicResource PrimaryHueMidBrush}"
                               TextTrimming="CharacterEllipsis"/>
                </StackPanel>

                <Button Grid.Column="1"
                        x:Name="ReaderModeButton"
                        Content="Reader Mode"
                        Click="ReaderModeButton_Click"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="0,0,8,0"
                        VerticalAlignment="Top"/>

                <Button Grid.Column="2"
                        x:Name="OpenOriginalButton"
                        Content="Open Original"
                        Click="OpenOriginalButton_Click"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        VerticalAlignment="Top"/>
            </Grid>
        </Border>

        <Border Grid.Row="1"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="1"
                CornerRadius="8">
            <wv:WebView2 x:Name="ArticleContentWebView"
                         Margin="1"/>
        </Border>
    </Grid>
</Window>